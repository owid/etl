version: 2

build:
  os: ubuntu-20.04
  tools:
    python: "3.9"
  commands:
    - git submodule init
    - git submodule update
    - pip install -U poetry
    - poetry install
    - poetry run mkdocs build
    - cp -r site/ _readthedocs/

submodules:
  include: all
