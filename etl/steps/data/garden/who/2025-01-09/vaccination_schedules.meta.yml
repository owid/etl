# NOTE: To learn more about the fields, hover over their names.
definitions:
  common:
    presentation:
      topic_tags:
        - Vaccination

# Learn more about the available fields:
# http://docs.owid.io/projects/etl/architecture/metadata/reference/
dataset:
  update_period_days: 365

tables:
  vaccination_schedules:
    variables:
      intro:
        title: Vaccination schedule for <<description.lower()>>
        description_short: The introduction status of <<description.lower()>> vaccination schedule.
        unit: ""
