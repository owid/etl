# NOTE: To learn more about the fields, hover over their names.
definitions:
  common:
    presentation:
      topic_tags:
        - Influenza

# Learn more about the available fields:
# http://docs.owid.io/projects/etl/architecture/metadata/reference/
dataset:
  update_period_days: 365

tables:
  flu_yamagata:
    variables:
      byam:
        title: Reported cases of the B Yamagata strain of influenza
        description_short: Reported number of B Yamagata influenza cases.
        unit: cases
        display:
          numDecimalPlaces: 0
