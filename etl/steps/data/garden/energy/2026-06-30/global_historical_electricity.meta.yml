# NOTE: To learn more about the fields, hover over their names.
definitions:
  common:
    presentation:
      topic_tags:
        - Energy


# Learn more about the available fields:
# http://docs.owid.io/projects/etl/architecture/metadata/reference/
dataset:
  update_period_days: 365


  owners:
    - Pablo Rosado
tables:
  global_historical_electricity:
    variables:
      # This is an intermediate dataset (Pinto's historical data), combined with modern data in the
      # electricity_mix step, so we don't define metadata for its variables here.
        {}
