# NOTE: To learn more about the fields, hover over their names.
definitions:
  common:
    presentation:
      topic_tags:
        - COVID-19


# Learn more about the available fields:
# http://docs.owid.io/projects/etl/architecture/metadata/reference/
dataset:
  update_period_days: 365


tables:
  user_contributions:
    variables:
      number_distinct_users:
        title: Users contributing to the repository
        unit: users
        short_unit: users
        description_short: Number of users at least contributing to the repository by creating an issue or a pull requests.

