# NOTE: To learn more about the fields, hover over their names.
definitions:
  common:
    presentation:
      topic_tags:
        - Forests & Deforestation

# Learn more about the available fields:
# http://docs.owid.io/projects/etl/architecture/metadata/reference/
dataset:
  update_period_days: 365

tables:
  fra_forest_area:
    variables:
      forest_share:
        title: Forest cover
        description_short: "The share of land area covered by forests."
        unit: "%"
        short_unit: "%"
        display:
          numDecimalPlaces: 1
        description_processing: "This variable is derived from the forest area variable in the FAO Global Forest Resources Assessment (FRA) 2020 data and the total land area variable from the FAO Land Use data."
