# NOTE: To learn more about the fields, hover over their names.
definitions:
  common:
    presentation:
      topic_tags:
        - Vaccination

# Learn more about the available fields:
# http://docs.owid.io/projects/etl/architecture/metadata/reference/
dataset:
  update_period_days: 365

tables:
  measles_deaths_census_bureau:
    variables:
      deaths:
        title: Number of measles deaths
        unit: deaths
        description_short: Reported number of measles deaths in the United States.
        display:
          numDecimalPlaces: 0
