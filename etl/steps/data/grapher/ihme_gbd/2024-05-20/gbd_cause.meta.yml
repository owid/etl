tables:
  gbd_cause_deaths:
    # TODO: move it to garden
    dimensions:
      - name: Cause of death
        slug: cause
        description: TBD cause
      - name: Age group
        slug: age
        description: TBD age
      - name: Metric
        slug: metric
        description: TBD metric
      # TODO: should we add `choices`? They could be added automatically from data when saving dataset.
