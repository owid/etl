from pathlib import Path

CURRENT_DIR = Path(__file__).parent
# Version of current grapher datasets to be created.
VERSION = str(CURRENT_DIR.name)
# Version of garden datasets to be imported.
GARDEN_VERSION = VERSION
